---
phase: 05-image-handling-sync-ui
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java
autonomous: true
requirements:
  - SYNC-07
  - SYNC-09
gap_closure: true
must_haves:
  truths:
    - "CSDN images in article content are downloaded to local filesystem storage"
    - "Images with duplicate URLs (same URL hash/MD5) are not re-downloaded"
  artifacts:
    - path: "backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java"
      provides: "Fixed RestClient usage — uses static factory instead of null-injected bean"
      contains: "RestClient.create()"
  key_links:
    - from: "ImageDownloadServiceImpl.java"
      to: "RestClient"
      via: "static factory"
      pattern: "private static final RestClient restClient = RestClient.create()"
---

<objective>
Fix ImageDownloadServiceImpl RestClient.Builder NPE — same bug that plan 05-05 fixed in CsdnArticleFetcherImpl. The `@Autowired RestClient.Builder restClientBuilder` at line 44 is null at runtime because no such Spring bean exists. Replace with `RestClient.create()` static factory. Once fixed, the MD5 deduplication check at line 99 becomes reachable and functional.
</objective>

<context>
@backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java
@backend/src/main/java/com/blog/service/impl/CsdnArticleFetcherImpl.java

## Gap Being Closed

From 05-VERIFICATION.md:
- **Gap 1:** ImageDownloadServiceImpl uses `@Autowired RestClient.Builder restClientBuilder` (line 44) which is null at runtime — same NPE bug that CsdnArticleFetcherImpl had before plan 05-05
- **Gap 2:** MD5 dedup logic at line 99 (`findByUrlHash`) is unreachable because the HTTP call fails with NPE before reaching it
- **Root cause:** No `RestClient.Builder` bean exists in the codebase; Spring only resolves @Autowired at startup, not compile time

## Reference Fix (plan 05-05 applied to CsdnArticleFetcherImpl)

`CsdnArticleFetcherImpl.java` line 19:
```java
private static final RestClient restClient = RestClient.create();
```

This is the exact pattern to apply to ImageDownloadServiceImpl.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix RestClient.Builder NPE in ImageDownloadServiceImpl</name>
  <read_first>backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java</read_first>
  <action>
Apply the same RestClient fix that plan 05-05 used in CsdnArticleFetcherImpl. The goal is to replace null-injected RestClient.Builder with a static RestClient factory.

**Changes to `backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java`:**

1. **Remove imports** (lines no longer needed):
   - Remove line 6: `import jakarta.annotation.PostConstruct;`
   - Remove line 9: `import org.springframework.beans.factory.annotation.Autowired;`
   - Remove line 12: `import org.springframework.http.client.SimpleClientHttpRequestFactory;`

2. **Remove fields** (lines 43-44 and 49):
   - Remove line 43-44: `@Autowired private RestClient.Builder restClientBuilder;`
   - Remove line 49: `private RestClient restClient;`

3. **Remove init() method** (lines 51-57):
   - Remove the entire `init()` method with `@PostConstruct`

4. **Add static RestClient** after the `uploadPath` field (after line 41):
   ```java
   private static final RestClient restClient = RestClient.create();
   ```

5. **Update call site** at line 121:
   - Change: `ResponseEntity<byte[]> response = restClient.get().uri(url).retrieve().toEntity(byte[].class);`
   - This already uses `restClient` (the instance field) — after removing the instance field and adding the static field, the local variable name `restClient` will resolve to the static field. No code change needed at line 121 IF the static field is named `restClient` (not `REST_CLIENT` or similar).

**Verification after edit:**
- `grep -c "RestClient.create()" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` should return 1
- `grep -c "@Autowired RestClient.Builder" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` should return 0
- `grep -c "@PostConstruct" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` should return 0
- `grep -c "restClientBuilder" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` should return 0
  </action>
  <verify>
    <automated>cd backend && mvn compile -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RestClient.create()" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` returns 1
    - `grep -c "@Autowired RestClient.Builder" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` returns 0
    - `grep -c "@PostConstruct" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` returns 0
    - `grep -c "restClientBuilder" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` returns 0
    - `grep -c "private RestClient restClient;" backend/src/main/java/com/blog/service/impl/ImageDownloadServiceImpl.java` returns 0
    - Maven compile succeeds: `cd backend && mvn compile -q` returns no errors
  </acceptance_criteria>
  <done>ImageDownloadServiceImpl uses RestClient.create() — no null-injected bean, no NPE. MD5 dedup at line 99 is now reachable.</done>
</task>

</tasks>

<verification>
## Gap Closure Verification

After the fix, image download should work end-to-end:
1. Maven compile succeeds with no errors
2. ImageDownloadServiceImpl no longer references null RestClient.Builder
3. `RestClient.create()` is used as a static field
4. MD5 deduplication check at `findByUrlHash` is reachable (no NPE before it)
5. Image download flow: content → processImage() → downloadWithBackoff() → RestClient.get().uri().retrieve() works

## Manual Verification Steps

1. Start backend: `cd backend && mvn spring-boot:run`
2. Navigate to http://localhost:5173/admin/csdn-sync
3. Enter a CSDN user ID and click sync
4. Verify in logs: "Downloaded image {url} -> {localPath}" appears for image URLs in article content
5. Verify MD5 dedup: second sync of same article shows "Cache hit for URL hash" instead of re-downloading
</verification>

<success_criteria>
- ImageDownloadServiceImpl.java no longer references null RestClient.Builder
- `RestClient.create()` is used as a static field
- PostConstruct init() method is removed
- Code compiles without errors
- MD5 deduplication is reachable and functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-image-handling-sync-ui/05-07-SUMMARY.md`
</output>
