---
phase: '05'
plan: '04'
type: execute
wave: 2
depends_on:
  - '05-03'
files_modified:
  - frontend/src/views/admin/ArticleEditorView.vue
autonomous: true
requirements:
  - SYNC-13

must_haves:
  truths:
    - "Admin receives warning dialog when editing an article that was synced from CSDN"
    - "Admin must acknowledge warning by clicking '我已知悉' before accessing the editor"
  artifacts:
    - path: frontend/src/views/admin/ArticleEditorView.vue
      provides: Blocking warning modal for CSDN-sourced articles
      modifies: ArticleEditorView.vue onMounted + template
  key_links:
    - from: ArticleEditorView.vue
      to: article.source
      via: articleStore.fetchArticle() result
      pattern: source.*CSDN
---

<objective>
Add blocking warning modal to ArticleEditorView that appears when editing an article with `source: CSDN`. Per D-03, the modal blocks all editor access until the user acknowledges with "我已知悉" button.
</objective>

<execution_context>
@D:/0Document/project/blog-cc/.claude/get-shit-done/workflows/execute-plan.md
@D:/0Document/project/blog-cc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/views/admin/ArticleEditorView.vue
@.planning/phases/05-image-handling-sync-ui/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSDN article warning modal to ArticleEditorView</name>
  <files>frontend/src/views/admin/ArticleEditorView.vue</files>
  <action>
Modify `frontend/src/views/admin/ArticleEditorView.vue`:

**1. Add to script setup (after existing declarations):**

```javascript
const showSyncWarning = ref(false)
```

**2. Modify onMounted to check for CSDN article after fetching:**

After this existing block:
```javascript
if (article) {
  title.value = article.title
  content.value = article.content
  status.value = article.status
  categoryId.value = article.categoryId
  tags.value = article.tags?.map(t => t.name) || []
}
```

Add:
```javascript
// D-03: Check if synced article — show blocking warning modal
if (article.source === 'CSDN') {
  showSyncWarning.value = true
}
```

**3. Add warning modal to template:**

Add the following immediately after `<div class="flex min-h-screen bg-gray-900">` (line 88) and before `<AdminSidebar />`:

```html
<!-- D-03: Synced article warning modal — blocks editor access until acknowledged -->
<div v-if="showSyncWarning" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
  <div class="bg-gray-800 rounded-lg p-6 w-full max-w-md border border-gray-700">
    <h3 class="text-lg font-semibold text-yellow-400 mb-4">编辑警告</h3>
    <p class="text-gray-300 mb-6">该文章为同步文章，不建议编辑。更改将在下次同步时被覆盖。</p>
    <button
      @click="showSyncWarning = false"
      class="w-full px-4 py-2 bg-yellow-600 hover:bg-yellow-700 text-white rounded-lg"
    >
      我已知悉
    </button>
  </div>
</div>
```

This modal uses `v-if="showSyncWarning"` — when shown, it covers the entire screen with `fixed inset-0` and `z-50`, blocking all interaction with the editor behind it. User must click "我已知悉" to dismiss.
</action>
  <verify>
    <automated>grep -c "showSyncWarning" frontend/src/views/admin/ArticleEditorView.vue && grep -c "编辑警告" frontend/src/views/admin/ArticleEditorView.vue && grep -c "我已知悉" frontend/src/views/admin/ArticleEditorView.vue</automated>
  </verify>
  <done>ArticleEditorView shows blocking warning modal when editing article with source='CSDN'; modal must be dismissed with '我已知悉' button before editor is accessible</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| client-side | Modal visibility controlled by Vue reactivity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | I | ArticleEditorView | accept | Modal is UX warning only — does not prevent edit if user bypasses modal (non-security control) |

**Security controls implemented:**
- Modal is a UX advisory (D-03), not an access control mechanism
- Backend API still requires JWT authentication for article updates
</threat_model>

<verification>
- ArticleEditorView shows warning modal when article.source === 'CSDN'
- Modal blocks editor access (fixed overlay with z-50)
- Modal contains "编辑警告" heading and "该文章为同步文章，不建议编辑" body text
- Modal has "我已知悉" button that dismisses the modal
- Editor is inaccessible until modal is dismissed
</verification>

<success_criteria>
- Editing an article with source='CSDN' shows blocking warning modal immediately on page load
- Modal displays correct text per 05-UI-SPEC.md
- Modal dismisses only when user clicks "我已知悉"
- After dismissal, editor is fully accessible
- Normal (non-CSDN) articles open without modal
</success_criteria>

<output>
After completion, create `.planning/phases/05-image-handling-sync-ui/05-04-SUMMARY.md`
</output>
