---
phase: 04-core-sync-infrastructure
plan: '05'
type: execute
wave: 4
depends_on:
  - '04'
files_modified:
  - backend/src/main/resources/mapper/ArticleMapper.xml
autonomous: true
requirements:
  - SYNC-05
  - SYNC-08
user_setup: []
gap_closure: true

must_haves:
  truths:
    - "Articles synced from CSDN have source='CSDN' recorded in database"
    - "Duplicate CSDN articles are detected by csdn_article_id and skipped or updated (not duplicated)"
    - "Content hash is stored and used for update detection"
  artifacts:
    - path: "backend/src/main/resources/mapper/ArticleMapper.xml"
      provides: "INSERT statement includes source, csdn_article_id, content_hash columns"
      min_lines: 1
  key_links:
    - from: "CsdnSyncServiceImpl"
      to: "ArticleMapper.insert"
      via: "articleMapper.insert(article)"
      pattern: "articleMapper\\.insert"
---

<objective>
Fix the gap identified in 04-VERIFICATION.md: ArticleMapper.insert omits source, csdn_article_id, and content_hash columns. The ResultMap and dedup SQL methods were added in plan 04-03, but the INSERT statement itself was never updated.
</objective>

<execution_context>
@D:/0Document/project/blog-cc/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@backend/src/main/resources/mapper/ArticleMapper.xml (current state - INSERT at lines 29-32 omits the three columns)
@.planning/phases/04-core-sync-infrastructure/04-VERIFICATION.md (gap details)
@.planning/phases/04-core-sync-infrastructure/04-03-PLAN.md (prior plan showing the entity is already extended)

# Background from 04-VERIFICATION.md:
# - Article entity has source, csdnArticleId, contentHash fields (added in 04-03)
# - ArticleMapper.xml ResultMap includes these fields at lines 12-14
# - findByCsdnArticleId and updateContentAndHash SQL definitions are present
# - CsdnSyncServiceImpl sets these fields before calling articleMapper.insert()
# - BUT the INSERT statement (lines 29-32) only has (title, content, status, category_id)
# - Result: articles.source=NULL, articles.csdn_article_id=NULL, articles.content_hash=NULL
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ArticleMapper.insert to include source, csdn_article_id, content_hash</name>
  <files>backend/src/main/resources/mapper/ArticleMapper.xml</files>
  <read_first>backend/src/main/resources/mapper/ArticleMapper.xml</read_first>
  <acceptance_criteria>
    - ArticleMapper.xml INSERT statement includes source, csdn_article_id, content_hash columns
    - INSERT column list is: title, content, status, category_id, source, csdn_article_id, content_hash
    - INSERT VALUES list has corresponding #{source}, #{csdnArticleId}, #{contentHash} placeholders
    - MyBatis #{ } parameterization is used (no ${} interpolation)
  </acceptance_criteria>
  <action>
Replace the INSERT statement in ArticleMapper.xml (lines 29-32) from:

```xml
<insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO articles (title, content, status, category_id)
    VALUES (#{title}, #{content}, #{status}, #{categoryId})
</insert>
```

To:

```xml
<insert id="insert" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO articles (title, content, status, category_id, source, csdn_article_id, content_hash)
    VALUES (#{title}, #{content}, #{status}, #{categoryId}, #{source}, #{csdnArticleId}, #{contentHash})
</insert>
```

This fix ensures:
- articles.source = 'CSDN' (per SYNC-08)
- articles.csdn_article_id = the CSDN article ID (per SYNC-08)
- articles.content_hash = MD5 hash of content (per SYNC-05 dedup + D-03 MD5 hash)

The ResultMap already correctly maps these columns (lines 12-14 of the file). CsdnSyncServiceImpl already sets these fields on the Article entity before calling insert().
  </action>
  <verify>
    <automated>grep -A3 '<insert id="insert"' backend/src/main/resources/mapper/ArticleMapper.xml | grep -c "source, csdn_article_id, content_hash"</automated>
  </verify>
  <done>ArticleMapper.insert updated to persist source, csdn_article_id, and content_hash columns</done>
</task>

</tasks>

<verification>
- INSERT statement includes all three columns: `grep 'INSERT INTO articles' backend/src/main/resources/mapper/ArticleMapper.xml`
- Maven compiles: `cd backend && mvn compile -q`
</verification>

<success_criteria>
- ArticleMapper.insert includes source, csdn_article_id, content_hash columns
- New articles synced from CSDN will have source='CSDN', csdn_article_id populated, content_hash populated
- SYNC-05 (dedup) and SYNC-08 (source marking) will work correctly
- mvn compile succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-sync-infrastructure/04-05-SUMMARY.md`
</output>
