---
wave: 1
depends_on: []
requirements_addressed: [PREV-01]
files_modified:
  - frontend/src/components/public/ArticleCard.vue
autonomous: true
---

# Plan 06-01: Add Preview Button to ArticleCard

## Objective

在 ArticleCard 组件的 meta 行添加眼睛图标预览按钮，用户点击时触发 preview 事件。

## Tasks

### Task 1: Read Existing ArticleCard

<read_first>
frontend/src/components/public/ArticleCard.vue
</read_first>

<action>
读取 ArticleCard.vue，了解当前结构：
- template 中的 meta 行布局（标签、日期位置）
- script setup 结构
- 当前有哪些 emits
</action>

<acceptance_criteria>
- [ ] ArticleCard.vue 文件可读
- [ ] 确认 meta 行在 template 中的位置（行号）
- [ ] 确认 existing emits 列表
</acceptance_criteria>

### Task 2: Add Preview Button

<read_first>
frontend/src/components/public/ArticleCard.vue
</read_first>

<action>
在 ArticleCard.vue 的 meta 行中添加预览按钮，放在标签之后、日期之前：

```vue
<!-- Add before <time> element in meta line -->
<button
  @click="$emit('preview', article.id)"
  class="p-1.5 rounded hover:bg-gray-700 text-gray-400 hover:text-blue-400 transition-colors"
  title="快速预览"
>
  <!-- Eye icon (heroicons outline style) -->
  <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
  </svg>
</button>
```

按钮需要添加到 div.flex.flex-wrap.items-center.gap-2.mt-3 中。
</action>

<acceptance_criteria>
- [ ] 眼睛图标按钮出现在 meta 行（标签和日期之间）
- [ ] 按钮 hover 时背景变灰、图标变蓝
- [ ] 点击按钮触发 $emit('preview', article.id)
- [ ] 按钮有 title="快速预览" 属性
</acceptance_criteria>

### Task 3: Verify Button Works

<read_first>
frontend/src/components/public/ArticleCard.vue
</read_first>

<action>
确认：
1. 按钮可点击（@click 绑定到 $emit）
2. 事件名为 'preview'
3. 传递 article.id 作为参数
4. 与现有 router-link 元素布局兼容
</action>

<acceptance_criteria>
- [ ] grep "preview" ArticleCard.vue 返回结果
- [ ] grep "article.id" ArticleCard.vue 返回结果
- [ ] 按钮位于 .flex.items-center 容器内
</acceptance_criteria>

---

## Verification

1. 读取 ArticleCard.vue — 按钮存在于 meta 行
2. grep 'preview' ArticleCard.vue — 找到事件发射
3. grep 'eye\|Eye\|svg.*viewBox.*0.*24.*24' ArticleCard.vue — 找到 SVG 图标
4. 在浏览器检查 ArticleCard 是否有可点击的眼睛按钮

## must_haves

- ArticleCard 卡片右上角 meta 行有眼睛图标按钮
- 点击按钮 emit('preview', article.id)
- 按钮样式与整体 dark theme 一致