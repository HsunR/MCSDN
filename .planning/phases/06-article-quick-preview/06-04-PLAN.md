---
wave: 2
depends_on: [06-01, 06-02, 06-03]
requirements_addressed: [PREV-01, PREV-02, PREV-03, PREV-04, PREV-05, PREV-06]
files_modified: []
autonomous: true
---

# Plan 06-04: Verify All Features

## Objective

验证 Phase 6 所有功能正常工作：预览按钮、Modal 渲染、元信息显示、关闭功能、分页保持。

## Tasks

### Task 1: Verify ArticleCard Button

<read_first>
frontend/src/components/public/ArticleCard.vue
</read_first>

<action>
确认：
1. 预览按钮存在于 meta 行
2. 按钮有 eye icon SVG
3. @click 触发 $emit('preview', article.id)
4. 按钮 title="快速预览"
</action>

<acceptance_criteria>
- [ ] grep 'preview' ArticleCard.vue | grep 'emit'
- [ ] grep 'svg.*eye\|Eye' ArticleCard.vue 或 grep 'M15 12' ArticleCard.vue（眼睛图标 path）
- [ ] 按钮在 flex 容器内（与 tags、date 同一行）
</acceptance_criteria>

### Task 2: Verify Modal Component

<read_first>
frontend/src/components/public/ArticlePreviewModal.vue
</read_first>

<action>
确认：
1. markdown-it + highlight.js 配置存在
2. prose-invert 用于渲染内容
3. overlay 有 bg-black/60 backdrop-blur-sm
4. Modal 有 max-w-4xl max-h-[90vh]
5. close 按钮存在（X icon）
6. Escape key handler 存在
7. store.fetchArticle 调用存在
</action>

<acceptance_criteria>
- [ ] grep 'MarkdownIt\|markdown-it' ArticlePreviewModal.vue
- [ ] grep 'prose-invert' ArticlePreviewModal.vue
- [ ] grep 'bg-black/60' ArticlePreviewModal.vue
- [ ] grep 'max-w-4xl\|max-h-\[90vh\]' ArticlePreviewModal.vue
- [ ] grep 'keydown\|Escape' ArticlePreviewModal.vue
- [ ] grep 'fetchArticle' ArticlePreviewModal.vue
</acceptance_criteria>

### Task 3: Verify HomeView Integration

<read_first>
frontend/src/views/public/HomeView.vue
</read_first>

<action>
确认：
1. ArticlePreviewModal 导入
2. previewArticleId ref 存在
3. handlePreview 函数存在
4. ArticleTimeline 有 @preview 绑定
5. Modal 组件在 template 中
6. @close 绑定存在
</action>

<acceptance_criteria>
- [ ] grep 'import ArticlePreviewModal' HomeView.vue
- [ ] grep 'previewArticleId' HomeView.vue
- [ ] grep '@preview' HomeView.vue
- [ ] grep '@close' HomeView.vue
</acceptance_criteria>

### Task 4: End-to-End Test

<action>
在浏览器中测试完整流程：

1. 打开 http://localhost:5173/?page=1
2. 找到任意文章卡片，确认有眼睛图标按钮
3. 点击眼睛按钮 → Modal 打开，显示文章标题
4. 确认 Markdown 内容渲染（代码高亮）
5. 确认元信息显示（分类、标签、日期）
6. 点击 overlay 背景 → Modal 关闭
7. 确认仍在列表页，分页未变

（或在 devtools 检查元素确认结构正确）
</action>

<acceptance_criteria>
- [ ] ArticleCard 有 preview 按钮（.p-1.5 rounded hover:bg-gray-700）
- [ ] Modal overlay 存在（.fixed.inset-0.bg-black/60）
- [ ] Modal 容器存在（.max-w-4xl.max-h-[90vh]）
- [ ] 文章标题显示（.text-2xl.font-bold）
- [ ] Markdown 内容区域（.prose.prose-invert）
</acceptance_criteria>

---

## Verification Summary

| 需求 | 验证点 | 状态 |
|------|--------|------|
| PREV-01 | 预览按钮在 ArticleCard | grep 确认 |
| PREV-02 | Modal 打开显示内容 | Modal 组件存在 |
| PREV-03 | Markdown 渲染 + 代码高亮 | markdown-it + hljs 配置 |
| PREV-04 | 元信息（标题/分类/标签/日期） | template 有 v-if article?.title 等 |
| PREV-05 | 关闭（overlay + X + Escape） | @click.self + handleClose + keydown |
| PREV-06 | 分页保持 | 状态管理无页面刷新 |

## must_haves

- 所有 6 个需求都有对应的实现代码
- grep 验证找到关键代码片段
- 无 broken references（导入路径正确）