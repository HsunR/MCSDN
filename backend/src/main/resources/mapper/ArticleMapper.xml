<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.ArticleMapper">
    <resultMap id="ArticleResultMap" type="com.blog.entity.Article">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="categoryId" column="category_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="ArticleResultMap">
        SELECT id, title, status, category_id, created_at, updated_at
        FROM articles
        ORDER BY created_at DESC
    </select>

    <select id="findById" resultMap="ArticleResultMap">
        SELECT id, title, content, status, category_id, created_at, updated_at
        FROM articles
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO articles (title, content, status, category_id)
        VALUES (#{title}, #{content}, #{status}, #{categoryId})
    </insert>

    <update id="update">
        UPDATE articles
        SET title = #{title},
            content = #{content},
            status = #{status},
            category_id = #{categoryId}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM articles WHERE id = #{id}
    </delete>

    <select id="findTagsByArticleId" resultType="com.blog.entity.Tag">
        SELECT t.id, t.name, t.created_at
        FROM tags t
        INNER JOIN article_tags at ON t.id = at.tag_id
        WHERE at.article_id = #{articleId}
    </select>

    <insert id="insertArticleTag">
        INSERT INTO article_tags (article_id, tag_id)
        VALUES (#{articleId}, #{tagId})
    </insert>

    <delete id="deleteArticleTags">
        DELETE FROM article_tags WHERE article_id = #{articleId}
    </delete>
</mapper>
